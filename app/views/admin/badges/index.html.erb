<h1 class="text text-center"><%= t('.header')%></h1>

<p>
  <%= link_to t('.new'), new_admin_badge_path, class: 'btn btn-success' %>
</p>

<%= render 'shared/errors', resource: @badge %>

<table class="table table-striped text-center">
  <thead>
    <tr>
      <th><%= t('.title') %></th>
      <th><%= t('.rule_name') %></th>
      <th><%= t('.rule_value') %></th>
      <th><%= t('.picture') %></th>
      <th colspan='3'><%= t('.actions') %></th>
    </tr>
  </thead>

  <%= render @badges %>
</table>
