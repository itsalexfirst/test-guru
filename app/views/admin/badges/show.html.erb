<table class="table table-striped text-center">
  <thead>
    <tr>
      <th><%= t('.title') %></th>
      <th><%= t('.rule_name') %></th>
      <th><%= t('.rule_value') %></th>
      <th><%= t('.picture') %></th>
    </tr>
  </thead>

  <td class="align-middle"><%= @badge.title %></td>
  <td class="align-middle"><%= @badge.rule_name %></td>
  <td class="align-middle"><%= @badge.rule_value %></td>
  <td class="align-middle"><%= @badge.picture %></td>
</table>

<%= link_to t('.edit'), edit_admin_badge_path(@badge) %> |
<%= link_to t('.back'), admin_badges_path %>
