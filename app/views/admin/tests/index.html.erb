<h1 class="text text-center"><%= t('.header')%></h1>

<p>
  <%= link_to t('.new'), new_admin_test_path, class: 'btn btn-success' %>
</p>

<table class="table table-striped text-center">
  <thead>
    <tr>
      <th><%= t('.title') %></th>
      <th><%= t('.level') %></th>
      <th><%= t('.questions') %></th>
      <th colspan='3'><%= t('.actions') %></th>
    </tr>
  </thead>

  <%= render @tests %>
</table>
