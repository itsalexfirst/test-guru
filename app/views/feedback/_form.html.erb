 <%= form_with url: feedback_index_path, local: true do |form| %>
  <%= render 'shared/errors', resource: @feedback %>

  <p>
    <%= form.text_area :body, rows: 7, cols: 60 %>
  </p>


  <p><%= form.submit t(".submit"), class: 'btn btn-primary' %></p>
<% end %>
